<% folders.forEach((folder) => { %>
    <li>
        <a href="/<%=currentUser.id%>/folder/<%=folder.id%>">
            <h3><%=folder.name%></h3>
        </a>
        <a href="/<%=currentUser.id%>/folder/<%=folder.id%>/updateFolder">update</a>
        <form action="/<%=currentUser.id%>/folder/<%=folder.id%>/deleteFolder" method="post">
            <input type="text" value="<%=folder.id%>" readonly hidden required>
            <button type="submit">Delete</button>
        </form>
    </li>
<% }); %>